<UserControl x:Class="ColorPicker.GradualColorPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Name="_this">
    <ListBox ItemsSource="{Binding Path=BrushArray, ElementName=_this}" BorderThickness="0" BorderBrush="Transparent" Background="Transparent"
             SelectionChanged="ListBox_SelectionChanged">
        <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Vertical" />
            </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
        <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
                <Setter Property="Padding" Value="1" />
            </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Rectangle Fill="{Binding Path=Value}" Width="16" Height="16" />
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
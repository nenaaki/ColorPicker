<Window
    x:Class="Sample.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cp="clr-namespace:ColorPicker;assembly=ColorPicker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Sample"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <StackPanel Orientation="Horizontal">
        <cp:ColorPickerControl CurrentColor="{Binding Path=CurrentColor}"/>
        <cp:HexColorPicker
            Width="256"
            Height="256"
            CurrentColor="{Binding Path=CurrentColor}" />
        <Grid VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <cp:RingColorPicker
                Width="192"
                Height="192"
                BaseColor="{Binding BaseColor, Mode=OneWayToSource}"
                CurrentColor="{Binding Path=CurrentColor}" />
            <cp:TriangleColorPicker
                Width="128"
                Height="128"
                Margin="32,0,0,0"
                CurrentColor="{Binding CurrentColor}" />
            <cp:RgbEditor Grid.Row="1" CurrentColor="{Binding Path=CurrentColor}" />
        </Grid>
        <StackPanel Orientation="Vertical">
            <Border Background="Black">
                <TextBlock FontWeight="Bold" Foreground="White">基本色</TextBlock>
            </Border>
            <cp:GradualColorPickerMultiSlot
                Name="_multiSlot"
                BaseColors="{Binding Path=BaseColors, Mode=OneTime}"
                CurrentColor="{Binding Path=CurrentColor}"
                GroupLength="8" />
            <Border Background="Black">
                <TextBlock FontWeight="Bold" Foreground="White">拡張</TextBlock>
            </Border>
            <cp:GradualColorPickerSlot BaseColors="{Binding SelectedItem, ElementName=_multiSlot}" CurrentColor="{Binding Path=CurrentColor}" />
            <Border Background="Black">
                <TextBlock FontWeight="Bold" Foreground="White">最近使った色</TextBlock>
            </Border>
            <cp:SimpleColorPicker
                Width="192"
                CurrentColor="{Binding Path=CurrentColor}"
                ItemsSource="{Binding Path=BaseColors, Mode=OneTime}" />
        </StackPanel>
        <cp:GradualColorPicker
            BaseColor="Red"
            CurrentColor="{Binding Path=CurrentColor}"
            StepCount="12" />
    </StackPanel>
</Window>
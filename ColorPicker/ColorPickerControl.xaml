<UserControl
    x:Class="ColorPicker.ColorPickerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ColorPicker"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <StackPanel Orientation="Horizontal">
        <StackPanel Orientation="Vertical">
            <Border Background="Black">
                <TextBlock FontWeight="Bold" Foreground="White">基本色</TextBlock>
            </Border>
            <local:GradualColorPickerMultiSlot
                x:Name="_multiSlot"
                BaseColors="{Binding Path=BaseColors, Mode=OneTime}"
                CurrentColor="{Binding Path=CurrentColor}"
                GroupLength="8" />
            <Border Background="Black">
                <TextBlock FontWeight="Bold" Foreground="White">拡張</TextBlock>
            </Border>
            <local:GradualColorPickerSlot BaseColors="{Binding SelectedItem, ElementName=_multiSlot}" CurrentColor="{Binding Path=CurrentColor}" />
            <Border Background="Black">
                <TextBlock FontWeight="Bold" Foreground="White">最近使った色</TextBlock>
            </Border>
            <local:SimpleColorPicker
                Width="192"
                CurrentColor="{Binding Path=CurrentColor}"
                ItemsSource="{Binding Path=BaseColors, Mode=OneTime}" />
        </StackPanel>
        <TabControl Background="Transparent">
            <TabItem Header="代表色">
                <local:HexColorPicker
                    Width="192"
                    Height="192"
                    CurrentColor="{Binding Path=CurrentColor}" />
            </TabItem>
            <TabItem Header="詳細">
                <Grid VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <local:RingColorPicker
                        Width="192"
                        Height="192"
                        BaseColor="{Binding BaseColor, Mode=OneWayToSource}"
                        CurrentColor="{Binding Path=CurrentColor}" />
                    <local:TriangleColorPicker
                        Width="128"
                        Height="128"
                        Margin="32,0,0,0"
                        CurrentColor="{Binding CurrentColor}" />
                    <local:RgbEditor Grid.Row="1" CurrentColor="{Binding Path=CurrentColor}" />
                </Grid>
            </TabItem>
        </TabControl>
    </StackPanel>
</UserControl>
